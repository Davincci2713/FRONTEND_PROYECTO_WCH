import 'package:flutter/material.dart';

class AppColors {
  // Paleta principal
  static const primary = Color(0xFF00341C);
  static const primaryDark = Color(0xFF001A0E);
  static const accentRed = Color(0xFFCC0000);
  static const accentBlue = Color(0xFF0033A0);
  static const accentYellow = Color(0xFFFFCC00);
  static const accentGreen = Color(0xFF009900);

  // Superficie
  static const surface = Color(0xFF0A0A0A);
  static const surfaceCard = Color(0xFF141414);
  static const surfaceElevated = Color(0xFF1E1E1E);

  // Texto
  static const onSurface = Color(0xFFF5F5F5);
  static const onSurfaceMuted = Color(0xFF9E9E9E);
  static const divider = Color(0xFF2A2A2A);

  // Gradiente hero
  static const List<Color> heroGradient = [
    Color(0xFF000000),
    Color(0x00000000),
  ];
}
